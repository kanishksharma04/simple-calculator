# Simple Calculator

A lightweight, dependency-free calculator built with plain HTML, CSS, and JavaScript.

## Scope

- Basic arithmetic: addition, subtraction, multiplication, division
- Decimal number support
- Clear and delete (backspace) actions
- Mouse and keyboard input
- Responsive, dark-themed UI

## Tech stack

No frameworks, no build step, no dependencies — just static files opened directly in a browser.
